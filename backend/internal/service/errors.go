package service

// This package's typed errors are defined in apperr; the console services
// return apperr values so handlers render stable customer-facing responses.
// (This file exists to keep the service package self-documenting; add
// service-specific error constructors here if a future slice needs them.)
